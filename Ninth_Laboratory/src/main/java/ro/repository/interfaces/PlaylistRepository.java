package ro.repository.interfaces;

import ro.entity.Playlist;

/**
 * Interface that provides all the specific (and CRUD) operations performed on the {@link Playlist} repository.
 */
public interface PlaylistRepository extends Repository<Playlist> {
}
